@import 'variables/variables.styl';

.menu {
	padding: 0 10px
	
	&-wrapper {

	}
	
	&-list {
		&__item {
			display block
			text-align center
			
			&-link {
				display inline-block
				width 100%
				height 100%
			}
		}
	}
}